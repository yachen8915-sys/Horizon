# Horizon「平台变化雷达」续聊交接卡

## 当前目标

【已确认】

修复「平台变化雷达」中小红书与 B 站官方页面无法读取的问题，同时：

- 兼容已经建立的生产 baseline；
- 不清空或重建现有缓存；
- 不重复触发完整日报；
- 不调用 AI；
- 不发送飞书测试消息；
- 已完成本地测试和只读 Smoke，相关修改已 Commit、Push。

## 已确认内容

【已确认】

- 正式运行 Run ID：`31554080940`
- Run Number：`13`
- 修复提交：`b828cd439fe335ff4e2ce9d98cab7ea0510c0f71`
- 正式运行结果：成功
- 正式飞书日报：已发送
- Platform Changes：采集到 0 条
- 已创建生产缓存：`platform-change-state-31554080940`
- 当前生产 baseline 已建立，状态结构仍为 v1。

【已确认】

本地已完成以下修复：

- 小红书改为读取同域官方公开规则 API；
- B 站从动态发现的官方版本化 JS 中提取社区公约正文；
- B 站只对规范化后的正文进行差异判断，不比较整个 JS 文件；
- 保留原有 watcher 名称及既有 search watcher 状态；
- 增加不调用 AI、不发送 Webhook、不修改生产状态的 `platform_changes_smoke` 模式；
- GitHub Actions 增加独立手动 Smoke 入口，要求真实生产 cache restore 成功后才执行。
- GitHub schedule 增加北京时间当日成功运行门禁，避免本机 fallback 成功后延迟 schedule 再次发送日报。

【已确认】

本地验证结果：

- full pytest：649 项通过；
- Python compileall：通过；
- 配置模型校验：通过；
- `git diff --check`：通过；
- 本地 Smoke：8 个 watcher 均成功，0 warning，0 条变化；
- `ai_called=false`；
- `webhook_sent=false`；
- `production_state_unchanged=true`；
- 本地没有生成或覆盖正式 `data/platform_change_state.json`。

## 当前最新版本

【已确认】

已发布版本：

- 分支：`main`
- 本地 HEAD 与 `origin/main`：均为 `b828cd4`
- 提交说明：`修复平台变化采集与日报重复发送`

【已确认】

本轮已提交文件：

- `.github/workflows/daily-summary.yml`
- `data/config.github.json`
- `pyproject.toml`
- `src/models.py`
- `src/scrapers/platform_changes.py`
- `src/platform_changes_smoke.py`
- `tests/test_github_runtime_config.py`
- `tests/test_platform_changes.py`
- `tests/test_platform_changes_smoke.py`
- `scripts/check_daily_run_gate.py`
- `tests/test_daily_run_gate.py`

【已确认】

工作区还有招聘、矿业雷达相关的未跟踪文件。这些属于其他工作，不得包含进本次平台变化修复提交。

## 已否定方向

【已确认】

- 不清空、删除或人为重建生产 baseline；
- 不把状态从 v1 直接升级为不兼容的 v2；
- 不制造假页面变化或模拟生产数据；
- 不用完整日报运行验证 watcher；
- 不重复触发 GitHub Actions 正式任务；
- 不发送飞书测试消息；
- 不把小红书、B 站的 JavaScript 壳当成真实规则内容；
- 不为了确认修复而立即进行第二次正式补跑。

## 尚未解决

【待验证】

- 本地没有 GitHub 云端缓存，因此尚未验证 Smoke 是否能实际恢复 `platform-change-state-31554080940`；
- 本轮相关修改已 Commit、Push：`b828cd439fe335ff4e2ce9d98cab7ea0510c0f71`；
- 重复日报门禁已完成本地回归，尚未在云端自然 schedule 中验证；
- 云端 Smoke 尚未运行；
- 新的小红书、B 站 watcher 首次随自然日报运行时，应只建立自身 baseline、不产生旧闻卡片，该行为仍需自然运行验证；
- GitHub Cache 第二天是否正常 restore、真实变化次日是否去重，仍需后续自然日更验证。

【你的推测】

现有生产缓存中，小红书和 B 站 watcher 因上次跳过而没有有效快照。修复后的首次自然运行会为这两个 watcher 建立 baseline，同时保留其他 watcher 的既有状态，不应将历史内容集中推送。

## 下一步

【待确认】

1. 仅在用户明确同意时触发一次 `platform_changes_smoke`，不触发完整日报。
2. 核对云端 Smoke 是否恢复 v1 生产缓存、8 个 watcher 是否正常、生产状态是否未改变，以及是否完全没有 AI、Webhook 和正式日报行为。
3. 此后停止人工运行，只通过后续自然日更验证真实变化和跨日去重。
